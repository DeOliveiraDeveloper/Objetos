const user = {
    nome: "De Oliveira",
    pst_grad: "Cb",
    tempoDeServico: "Infinitos anos"
}

// Toda vez que precisar acessar algo dentro do objeto, basta coloca "objeto.algo"

// Atribuir um novo valor ao atributo pst_grad
user.pst_grad = "Sgt";

    console.log(user.pst_grad);